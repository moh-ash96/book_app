<!DOCTYPE html>
<html lang="en">


<%- include('../layout/head', {title:'Results'}) %>

    <body>
        <div class="wrapper">
            <main class="show-search">
                <%- include('../layout/header') %>

                    <div class="searchBy">
                        <h2>Search Results</h2>
                    </div>

                    <% searchResults.forEach(book=>{ %>
                        <%- include('../books/detail', {book}) %>
                            <% }) %>
            </main>
            <div class="push"></div>
            <%- include('../layout/footer') %>
        </div>
    </body>

</html>